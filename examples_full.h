#ifndef EXAMPLES_H
#define EXAMPLES_H

/* Parte 1 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 2 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 3 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 4 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 5 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 6 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 7 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 8 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 9 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 10 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 11 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 12 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 13 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 14 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 15 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 16 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 17 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 18 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 19 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 20 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 21 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 22 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 23 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 24 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 25 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 26 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 27 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 28 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 29 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 30 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 31 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 32 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 33 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 34 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 35 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 36 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 37 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 38 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 39 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 40 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 41 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 42 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 43 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 44 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 45 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 46 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 47 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 48 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 49 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 50 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 51 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 52 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 53 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 54 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 55 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 56 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 57 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 58 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 59 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


/* Parte 60 */

/* 
Exemplo de manipulação de pixels
*/
// unsigned char r = image[(y * width + x) * channels + 0];
// unsigned char g = image[(y * width + x) * channels + 1];
// unsigned char b = image[(y * width + x) * channels + 2];
// unsigned char a = image[(y * width + x) * channels + 3];

/* 
Exemplo de blending de imagens
*/
// unsigned char* blended = stb_image_blend(image1, image2, width, height, channels);
// if (blended) stb_image_free(blended);

/* 
Exemplo de criação de máscara
*/
// unsigned char* mask = stb_image_create_mask(image, width, height, channels);
// if (mask) stb_image_free(mask);

/* 
Exemplo de compressão e descompressão
*/
// unsigned char* compressed = stb_image_compress(image, width, height, channels);
// if (compressed) stb_image_free(compressed);


#endif // EXAMPLES_H